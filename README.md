To better understand modern frameworks, I built a memory game using pure JavaScript and the Observer Pattern.

The Goal: Manage complex game state (turns, matching logic, scores) without a single library.

The Tech: ES6 Classes, Custom Subscribers, DOM Manipulation.
